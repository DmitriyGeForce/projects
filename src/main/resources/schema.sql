DROP TABLE places IF EXISTS;
CREATE TABLE places (
    id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
    place_name VARCHAR(127) NOT NULL,
    place_geo_lat DOUBLE NOT NULL,
    place_geo_long DOUBLE NOT NULL,
    place_descript VARCHAR(65535)
);